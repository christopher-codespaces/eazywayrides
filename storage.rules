rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Drivers can read/write ONLY their own uploads
    match /driver-documents/{uid}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
